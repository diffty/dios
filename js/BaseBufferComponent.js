class BaseBufferComponent {
    constructor() {
        this.buffer = "";
    }

    setBuffer(newBuffer) {
        this.buffer = newBuffer;
    }

    getBuffer() {
        return this.buffer;
    }

    update(deltaTime) {
        
    }

    draw(deltaTime) {
        
    }
}
